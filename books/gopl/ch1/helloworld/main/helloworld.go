package main // Notice that we created a directory (i.e., package) "main"

import "fmt" // () are optional

func main() { // `func` is the keyword used to define a function
	// Brace placement is the same as one would use in Java
	fmt.Println("Hello, 世界") // No ending ';'
}
