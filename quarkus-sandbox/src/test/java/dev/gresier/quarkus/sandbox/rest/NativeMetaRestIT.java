package dev.gresier.quarkus.sandbox.rest;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMetaRestIT extends MetaRestTest {

    // Execute the same tests but in native mode
}
